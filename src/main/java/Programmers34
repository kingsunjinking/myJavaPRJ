class Solution {
    public int solution(int a, int d, boolean[] included) {
        int ans = 0;
        int c = a;
        for(int i = 0; i<included.length; i++){
            if(included[i] == true){
                ans += c;
            }
            c += d;
        }
        return ans;
    }
}
